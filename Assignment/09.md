# 76 슬라이드 정리
```c
#include <stdio.h> //표준 입출력 함수를 사용하기 위한 헤더 파일
#include <stdlib.h> //표준 라이브러리의 함수를 사용하기 위한 헤더 파일

int main(void)
{
	system("dir"); //현재 디렉토리의 파일 및 폴더 목록을 출력

	printf("아무 키나 치세요\n");

	_getch(); //사용자가 아무 키나 누를 때까지 대기

	system("cls"); //화면을 지우고 콘솔 창을 초기화

	return 0;
}
```
